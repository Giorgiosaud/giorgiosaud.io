---
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  const pages = await getCollection("colecciones");
  return pages.map((page) => ({
    params: { slug: page.id },
    props: { page },
  }));
}
---
