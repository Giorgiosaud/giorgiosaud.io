---
import Contactform from "@components/contactform.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";
import { getLangFromUrl, useTranslatedPath } from "@i18n/utils";
import Layout from "@layouts/Layout.astro";
import { Icon } from "astro-icon/components";
const langToTranslate = getLangFromUrl(Astro.url)==="en"?"es":"en";
const {translatePath} = useTranslatedPath(langToTranslate);
const translateActualPath=translatePath('contact',langToTranslate);
---

<Layout title="Contactame para mas información" translateActualPath={translateActualPath}>
  <Container>
    <Sectionhead>
      <Fragment slot="title">Contáctame</Fragment>
      <Fragment slot="desc">Estoy para ayudarte.</Fragment>
    </Sectionhead>

    <div class="grid md:grid-cols-2 gap-10 mx-auto max-w-4xl mt-16">
      <div>
        <h2 class="font-medium text-2xl text-gray-800">Contacto Giorgiosaud</h2>
        <p class="text-lg leading-relaxed text-slate-500 mt-3">
          ¿Tienes algo que decir? Estoy aquí para ayudarte. Llena el formulario
          o envía un correo electrónico o llama por teléfono.
        </p>
        <div class="mt-5">
          <div class="flex items-center mt-2 space-x-2 text-gray-600">
            <Icon class="text-gray-400 w-4 h-4" name="uil:map-marker" />
            <span>Santiago de chile, CL</span>
          </div><div class="flex items-center mt-2 space-x-2 text-gray-600">
            <Icon class="text-gray-400 w-4 h-4" name="uil:envelope" /><a
              href="mailto:me@giorgiosaud.io">me@giorgiosaud.io</a
            >
          </div>
        </div>
      </div>
      <div>
        <Contactform />
      </div>
    </div>
  </Container>
</Layout>
