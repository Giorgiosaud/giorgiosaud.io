---
import CloudinaryImage from "./ui/cloudinaryImage.astro";

interface Props {
    link: string;
    imageSrc: string;
    imageAlt: string;
    index:number;
    slug:string;
    category:string;
    title: string;
    authorAlias?:string;
    authorLink:string;
    publishDate: Date;
    resume:string;
}
const { 
    link,
    imageSrc,
    imageAlt,
    index,
    slug,
    title,
    category,
    resume,
    authorAlias,
    authorLink,
    publishDate
} = Astro.props as Props;

console.log(Astro.props)
console.log(publishDate instanceof Date)
---
<div class="grid md:grid-cols-2 gap-5 md:gap-10 items-center">
    <a href={link}>
        <CloudinaryImage
            class="w-full rounded-md object-cover object-center bg-white aspect-square"
            src={`notebook-posts/${imageSrc}`}
            alt={imageAlt}
            aspectRatio="1"
            loading={index <= 2 ? "eager" : "lazy"}
            decoding={index <= 2 ? "sync" : "async"}
            transition:name=`${slug}_img`
        />
    </a>
    <div>
        <a href={link}>
        <span class="text-blue-400 uppercase tracking-wider text-sm font-medium"
            transition:name=`${slug} category`>
            {category}
        </span>
        <h2 class="text-3xl font-semibold leading-snug tracking-tight mt-1 "  transition:name=`${slug} title`>
            {title}
        </h2>
    </a>
    <div class="flex gap-2 mt-3 align-start"
        transition:name=`${slug} meta`>
        <a class="text-gray-400" href={authorLink}
            transition:name=`${authorAlias} alias`>
            {authorAlias}
        </a>
        <span class="text-gray-400">• </span>
        <time
            class="text-gray-400"
            datetime={publishDate.toISOString()}>
            {publishDate.toLocaleDateString()}
        </time>
    </div>
    <p class="text-slate-500">
        {resume}
    </p>
</div>
</div>
    